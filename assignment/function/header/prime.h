#ifndef PRIME_H_INCLUDED
#define PRIME_H_INCLUDED

/**
* Checks if the integer is prime or not
* @param[in] number for which  has to be checked
* @return Prime or not
* @note Returns 0 for not prime
*/
int primenum(int number);


#endif // PRIME_H_INCLUDED
